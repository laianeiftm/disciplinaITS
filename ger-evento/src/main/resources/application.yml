spring:
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/gerenciador_evento
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: true
  
  mvc:
    hiddenmethod:
      filter:
        enabled: true

